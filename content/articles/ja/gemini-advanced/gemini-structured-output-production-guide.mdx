---
title: "Gemini構造化出力の実践ガイド — JSONスキーマで信頼性の高いAI出力を実現する"
slug: "gemini-structured-output-production-guide"
category: "gemini-advanced"
level: "advanced"
premium: true
date: "2026-03-24T18:00"
author: "Gemini Lab"
description: "Gemini APIの構造化出力（Structured Output）を本番環境で活用するための実践ガイド。JSONスキーマ定義、バリデーション、エラーハンドリング、複雑なデータ抽出パイプラインの構築方法を解説します。"
tags: ["gemini", "structured-output", "json-schema", "api", "production"]
highlights:
  - "Gemini Structured Output本番環境実装ガイド"
  - "確実なデータ形式制御とバリデーション"
  - "API設計とスキーマ管理のベストプラクティス"

---

## Gemini構造化出力とは — AIの出力を「型安全」にする技術

Gemini APIの構造化出力（Structured Output）は、AIの応答を事前に定義したJSONスキーマに強制的に従わせる機能です。従来の自由形式テキスト出力と異なり、確実にパース可能なJSONを得られるため、本番環境のパイプラインに組み込む際に圧倒的な信頼性を発揮します。

「AIの出力をプログラムで処理したいのに、フォーマットが毎回微妙に違う」——この問題を根本的に解決するのが構造化出力です。

この記事では、基本的なスキーマ定義から、ネストされた複雑なデータ構造、バリデーション戦略、そして実際の業務で使えるデータ抽出パイプラインまで、段階的に解説します。

## 基本的なスキーマ定義と使い方

まずは最もシンプルな構造化出力から始めましょう。

```typescript
// basic-structured-output.ts
import { GoogleGenerativeAI, SchemaType } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY!);

// スキーマ定義 — AIの出力形式を厳密に指定
const productReviewSchema = {
  type: SchemaType.OBJECT,
  properties: {
    product_name: {
      type: SchemaType.STRING,
      description: "レビュー対象の製品名",
    },
    rating: {
      type: SchemaType.NUMBER,
      description: "1〜5の評価スコア",
    },
    pros: {
      type: SchemaType.ARRAY,
      items: { type: SchemaType.STRING },
      description: "良い点のリスト",
    },
    cons: {
      type: SchemaType.ARRAY,
      items: { type: SchemaType.STRING },
      description: "改善点のリスト",
    },
    summary: {
      type: SchemaType.STRING,
      description: "100文字以内の総評",
    },
    recommended: {
      type: SchemaType.BOOLEAN,
      description: "おすすめかどうか",
    },
  },
  required: [
    "product_name", "rating", "pros", "cons", "summary", "recommended"
  ],
};

async function analyzeReview(reviewText: string) {
  const model = genAI.getGenerativeModel({
    model: "gemini-2.5-pro",
    generationConfig: {
      responseMimeType: "application/json",
      responseSchema: productReviewSchema,
    },
  });

  const result = await model.generateContent(
    `以下のレビューテキストを分析してください:\n\n${reviewText}`
  );

  // 型安全なJSONが返る — パースエラーの心配なし
  const review = JSON.parse(result.response.text());
  console.log(`製品: ${review.product_name}`);
  console.log(`評価: ${"★".repeat(review.rating)}${"☆".repeat(5 - review.rating)}`);
  console.log(`おすすめ: ${review.recommended ? "はい" : "いいえ"}`);

  return review;
}
```

`responseMimeType: "application/json"` と `responseSchema` を組み合わせることで、Geminiは必ず指定したスキーマに従ったJSONを出力します。

## ネストされた複雑なスキーマ

実務では、フラットなオブジェクトだけでは足りません。ネストされた構造やEnum型を使った高度なスキーマの例を見てみましょう。

```typescript
// complex-schema.ts — ネストされたスキーマ定義
const invoiceExtractionSchema = {
  type: SchemaType.OBJECT,
  properties: {
    invoice_number: {
      type: SchemaType.STRING,
      description: "請求書番号",
    },
    date: {
      type: SchemaType.STRING,
      description: "発行日（YYYY-MM-DD形式）",
    },
    vendor: {
      type: SchemaType.OBJECT,
      properties: {
        name: { type: SchemaType.STRING },
        address: { type: SchemaType.STRING },
        tax_id: { type: SchemaType.STRING },
      },
      required: ["name"],
      description: "発行元の情報",
    },
    line_items: {
      type: SchemaType.ARRAY,
      items: {
        type: SchemaType.OBJECT,
        properties: {
          description: {
            type: SchemaType.STRING,
            description: "商品・サービスの説明",
          },
          quantity: {
            type: SchemaType.NUMBER,
            description: "数量",
          },
          unit_price: {
            type: SchemaType.NUMBER,
            description: "単価（税抜）",
          },
          tax_rate: {
            type: SchemaType.NUMBER,
            description: "税率（例: 0.10 = 10%）",
          },
          total: {
            type: SchemaType.NUMBER,
            description: "小計（税込）",
          },
        },
        required: ["description", "quantity", "unit_price", "total"],
      },
      description: "明細行",
    },
    subtotal: {
      type: SchemaType.NUMBER,
      description: "税抜合計",
    },
    tax_total: {
      type: SchemaType.NUMBER,
      description: "税額合計",
    },
    grand_total: {
      type: SchemaType.NUMBER,
      description: "税込合計",
    },
    payment_status: {
      type: SchemaType.STRING,
      enum: ["paid", "pending", "overdue"],
      description: "支払い状況",
    },
    currency: {
      type: SchemaType.STRING,
      enum: ["JPY", "USD", "EUR"],
      description: "通貨",
    },
  },
  required: [
    "invoice_number", "date", "vendor", "line_items",
    "grand_total", "payment_status", "currency"
  ],
};
```

## バリデーション層の構築

構造化出力はスキーマに従ったJSONを保証しますが、ビジネスロジックレベルのバリデーションは別途必要です。

```typescript
// validation.ts — ビジネスロジックバリデーション
import { z } from 'zod';

// Zodスキーマでランタイムバリデーション
const InvoiceSchema = z.object({
  invoice_number: z.string().min(1),
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  vendor: z.object({
    name: z.string().min(1),
    address: z.string().optional(),
    tax_id: z.string().optional(),
  }),
  line_items: z.array(z.object({
    description: z.string(),
    quantity: z.number().positive(),
    unit_price: z.number().min(0),
    tax_rate: z.number().min(0).max(1).optional(),
    total: z.number().min(0),
  })).min(1),
  subtotal: z.number().min(0).optional(),
  tax_total: z.number().min(0).optional(),
  grand_total: z.number().min(0),
  payment_status: z.enum(["paid", "pending", "overdue"]),
  currency: z.enum(["JPY", "USD", "EUR"]),
});

type Invoice = z.infer<typeof InvoiceSchema>;

// 整合性チェック — 明細の合計と総額が一致するか検証
function validateInvoiceConsistency(invoice: Invoice): {
  valid: boolean;
  errors: string[];
} {
  const errors: string[] = [];

  // 明細の合計を計算
  const calculatedTotal = invoice.line_items.reduce(
    (sum, item) => sum + item.total, 0
  );

  // 総額との差異をチェック（浮動小数点誤差を考慮）
  if (Math.abs(calculatedTotal - invoice.grand_total) > 1) {
    errors.push(
      `明細合計(${calculatedTotal})と総額(${invoice.grand_total})が一致しません`
    );
  }

  // 数量チェック
  for (const item of invoice.line_items) {
    if (item.quantity !== Math.floor(item.quantity) && item.quantity < 0) {
      errors.push(`不正な数量: ${item.description} (${item.quantity})`);
    }
  }

  return { valid: errors.length === 0, errors };
}

// 使用例
async function processInvoice(rawText: string) {
  // Step 1: Geminiで構造化抽出
  const extracted = await extractInvoiceWithGemini(rawText);

  // Step 2: Zodでスキーマバリデーション
  const parsed = InvoiceSchema.safeParse(extracted);
  if (!parsed.success) {
    console.error("スキーマエラー:", parsed.error.format());
    throw new Error("Invalid invoice structure");
  }

  // Step 3: ビジネスロジックバリデーション
  const consistency = validateInvoiceConsistency(parsed.data);
  if (!consistency.valid) {
    console.warn("整合性警告:", consistency.errors);
    // 警告として記録するが処理は続行
  }

  return parsed.data;
}
```

## データ抽出パイプラインの構築

複数のドキュメントを一括処理するパイプラインを構築します。

```typescript
// pipeline.ts — データ抽出パイプライン
interface PipelineConfig {
  concurrency: number;     // 同時処理数
  retryLimit: number;      // リトライ上限
  batchSize: number;       // バッチサイズ
}

class ExtractionPipeline<T> {
  private config: PipelineConfig;
  private results: Map<string, {
    status: 'success' | 'error';
    data?: T;
    error?: string;
  }> = new Map();

  constructor(
    private schema: any,
    private validator: (data: unknown) => T,
    config?: Partial<PipelineConfig>
  ) {
    this.config = {
      concurrency: 3,
      retryLimit: 2,
      batchSize: 10,
      ...config,
    };
  }

  async process(
    documents: { id: string; content: string }[]
  ): Promise<Map<string, typeof this.results extends Map<string, infer V> ? V : never>> {
    // バッチに分割
    const batches = this.chunk(documents, this.config.batchSize);

    for (const batch of batches) {
      // 同時実行数を制限しながら処理
      const semaphore = new Semaphore(this.config.concurrency);

      await Promise.all(
        batch.map(async (doc) => {
          await semaphore.acquire();
          try {
            const result = await this.processDocument(doc);
            this.results.set(doc.id, {
              status: 'success',
              data: result,
            });
          } catch (error) {
            this.results.set(doc.id, {
              status: 'error',
              error: error instanceof Error ? error.message : String(error),
            });
          } finally {
            semaphore.release();
          }
        })
      );
    }

    return this.results;
  }

  private async processDocument(
    doc: { id: string; content: string }
  ): Promise<T> {
    let lastError: Error | null = null;

    for (let attempt = 0; attempt <= this.config.retryLimit; attempt++) {
      try {
        const model = genAI.getGenerativeModel({
          model: "gemini-2.5-pro",
          generationConfig: {
            responseMimeType: "application/json",
            responseSchema: this.schema,
          },
        });

        const result = await model.generateContent(
          `以下のドキュメントからデータを抽出してください:\n\n${doc.content}`
        );

        const parsed = JSON.parse(result.response.text());
        return this.validator(parsed);
      } catch (error) {
        lastError = error instanceof Error ? error : new Error(String(error));
        if (attempt < this.config.retryLimit) {
          // 指数バックオフで待機
          await new Promise(r =>
            setTimeout(r, 1000 * Math.pow(2, attempt))
          );
        }
      }
    }

    throw lastError;
  }

  private chunk<U>(array: U[], size: number): U[][] {
    const chunks: U[][] = [];
    for (let i = 0; i < array.length; i += size) {
      chunks.push(array.slice(i, i + size));
    }
    return chunks;
  }
}

// セマフォの簡易実装
class Semaphore {
  private current = 0;
  private queue: (() => void)[] = [];

  constructor(private max: number) {}

  async acquire(): Promise<void> {
    if (this.current < this.max) {
      this.current++;
      return;
    }
    return new Promise<void>(resolve => {
      this.queue.push(resolve);
    });
  }

  release(): void {
    this.current--;
    const next = this.queue.shift();
    if (next) {
      this.current++;
      next();
    }
  }
}
```

## エラーハンドリングのベストプラクティス

構造化出力で発生しやすいエラーとその対処法を紹介します。

```typescript
// error-handling.ts — エラーハンドリングパターン
async function safeStructuredGeneration<T>(
  model: any,
  prompt: string,
  validator: (data: unknown) => T,
  options?: { maxRetries?: number; fallbackModel?: string }
): Promise<{ success: true; data: T } | { success: false; error: string }> {
  const maxRetries = options?.maxRetries ?? 2;

  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      const result = await model.generateContent(prompt);
      const text = result.response.text();

      // JSONパース
      let parsed: unknown;
      try {
        parsed = JSON.parse(text);
      } catch {
        // まれにJSONが壊れる場合のフォールバック
        // マークダウンコードブロックで囲まれている場合に対応
        const match = text.match(/```json\n?([\s\S]*?)\n?```/);
        if (match) {
          parsed = JSON.parse(match[1]);
        } else {
          throw new Error("Failed to parse JSON response");
        }
      }

      // バリデーション
      const validated = validator(parsed);
      return { success: true, data: validated };
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);

      if (attempt === maxRetries) {
        return { success: false, error: errorMsg };
      }

      // リトライ前にプロンプトを調整
      prompt = `${prompt}\n\n注意: 前回の応答でエラーが発生しました（${errorMsg}）。正しいJSON形式で回答してください。`;
    }
  }

  return { success: false, error: "Max retries exceeded" };
}
```

## パフォーマンス最適化のヒント

### スキーマの簡素化

スキーマが複雑すぎると処理時間が増加します。必要最小限のフィールドに絞りましょう。

```typescript
// ❌ 過剰なスキーマ — 不要なフィールドが多い
const overEngineeredSchema = {
  type: SchemaType.OBJECT,
  properties: {
    // 20以上のフィールド...
  }
};

// ✅ 必要十分なスキーマ
const optimizedSchema = {
  type: SchemaType.OBJECT,
  properties: {
    // コアフィールドのみ、後で拡張可能
    name: { type: SchemaType.STRING },
    value: { type: SchemaType.NUMBER },
    category: { type: SchemaType.STRING, enum: ["a", "b", "c"] },
  },
  required: ["name", "value"],
};
```

### レスポンスキャッシュの活用

同じ入力に対する結果をキャッシュして、APIコールを削減します。

```typescript
// キャッシュ付き構造化出力
const cache = new Map<string, { data: any; expiry: number }>();

async function cachedGeneration(
  prompt: string,
  schema: any,
  ttlMs: number = 3600000 // 1時間
) {
  const cacheKey = JSON.stringify({ prompt, schema });
  const cached = cache.get(cacheKey);

  if (cached && cached.expiry > Date.now()) {
    return cached.data;
  }

  const result = await generateStructuredOutput(prompt, schema);
  cache.set(cacheKey, { data: result, expiry: Date.now() + ttlMs });
  return result;
}
```

## よくある質問

### Q: 構造化出力はどのGeminiモデルで使えますか？

Gemini 2.5 Pro、Gemini 2.5 Flash、Gemini 2.0 Flashなど、主要なモデルで利用可能です。`responseMimeType: "application/json"` と `responseSchema` の両方を設定することで有効になります。

### Q: 構造化出力の制限はありますか？

スキーマの深さやプロパティ数に制限があります。一般的にはネスト5階層以内、プロパティ数100以内を目安にしてください。非常に複雑なスキーマは、複数のAPIコールに分割することを検討してください。

### Q: 構造化出力と通常のJSON出力指示の違いは何ですか？

通常のプロンプトで「JSONで返してください」と指示する場合、モデルが従わない可能性があります。構造化出力はAPIレベルでJSONスキーマを強制するため、100%パース可能なJSONが返ります。本番環境では構造化出力を使うべきです。

## まとめ — 構造化出力で信頼性の高いAIパイプラインを構築する

Geminiの構造化出力は、AIの出力を「使えるデータ」に変換するための最も信頼性の高い方法です。JSONスキーマで出力形式を厳密に定義し、Zodなどのバリデーションライブラリで整合性を検証し、リトライ戦略でエラーに対応する。この3層構造で、本番環境でも安定したAIデータパイプラインを構築できます。

まずはシンプルなスキーマから試して、徐々に複雑なデータ抽出に挑戦していきましょう。
