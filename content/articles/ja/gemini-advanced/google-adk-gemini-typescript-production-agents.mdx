---
title: "Google ADK × Gemini — TypeScript本番マルチエージェントシステム構築"
slug: "google-adk-gemini-typescript-production-agents"
category: "gemini-advanced"
level: "advanced"
premium: true
date: "2026-03-29T10:00"
author: "Gemini Lab"
description: "Google Agent Development Kit (ADK) と Gemini を組み合わせ、TypeScriptで堅牢なマルチエージェントシステムを本番環境で構築・運用するための完全ガイド"
tags: ["Google ADK", "マルチエージェント", "TypeScript", "Gemini API", "エージェント開発", "本番運用"]
highlights:
  - "Google ADK のアーキテクチャを深く理解し、TypeScript で型安全なマルチエージェントシステムを設計・実装できる"
  - "エージェント間通信・タスク委譲・エラー回復の本番パターンを習得し、信頼性の高いシステムを構築できる"
  - "コスト監視・レート制限・オブザーバビリティを統合した運用基盤の設計手法を学べる"
---

Google Agent Development Kit (ADK) は、AI エージェントの開発・デプロイを大幅に簡素化する Google の統合プラットフォームです。本記事では、ADK と Gemini API を組み合わせ、TypeScript で本番レベルのマルチエージェントシステムを構築する実践的な手法を解説します。

## Google ADK の全体像

### ADK の3つのコア機能

Google ADK は以下の3層で構成されています。

**エージェント定義レイヤー**

エージェントの基本ユニットとなる「Agent」クラスにより、以下を一元管理します。

- システムプロンプト（ロール定義）
- ツール（外部APIやDB連携）
- メモリ（セッションコンテキスト保持）
- 推論エンジン（Gemini）

**ワークフロー統合レイヤー**

複数エージェント間の協調を定義します。

- シーケンシャル処理（エージェントA → B → C）
- パラレル処理（複数エージェントの並行実行）
- 条件分岐（ルーティング、タスク委譲）
- 再試行・フォールバック

**実行・監視レイヤー**

本番環境での運用をサポート。

- ローカルテスト環境
- Cloudflare Workers / Cloud Run へのデプロイ
- オブザーバビリティ（トレース・ログ・メトリクス）
- コスト・レート管理

## ADK プロジェクトのセットアップ

### 環境構築

```bash
node --version  # v18.17.0 以上
npm --version   # v9.6.7 以上

mkdir gemini-adk-agents
cd gemini-adk-agents
npm init -y

npm install --save \
  @google/generative-ai \
  dotenv \
  zod
```

### TypeScript 設定

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

## エージェント設計の基本パターン

### 型安全なエージェント定義

```typescript
import { GoogleGenerativeAI } from "@google/generative-ai";
import { z } from "zod";

interface AgentConfig {
  name: string;
  role: string;
  systemPrompt: string;
  tools: Tool[];
  model: string;
}

class GeminiAgent {
  private client: GoogleGenerativeAI;
  private config: AgentConfig;
  private conversationHistory: Array<{
    role: "user" | "assistant";
    content: string;
  }> = [];

  constructor(apiKey: string, config: AgentConfig) {
    this.client = new GoogleGenerativeAI(apiKey);
    this.config = config;
  }

  async execute(userMessage: string): Promise<string> {
    this.conversationHistory.push({
      role: "user",
      content: userMessage,
    });

    const model = this.client.getGenerativeModel({
      model: this.config.model,
      systemInstruction: this.config.systemPrompt,
    });

    const response = await model.generateContent({
      contents: this.conversationHistory.map((msg) => ({
        role: msg.role,
        parts: [{ text: msg.content }],
      })),
    });

    const assistantMessage =
      response.response.content.parts[0].text || "";
    this.conversationHistory.push({
      role: "assistant",
      content: assistantMessage,
    });

    return assistantMessage;
  }

  resetContext(): void {
    this.conversationHistory = [];
  }
}
```

## マルチエージェントシステムの実装

### エージェント間通信パターン

**パターン1: 順序的委譲（Sequential Handoff）**

```typescript
interface TaskResult {
  success: boolean;
  data: Record<string, any>;
  context: string;
}

class OrchestrationEngine {
  private agents: Map<string, GeminiAgent>;

  constructor() {
    this.agents = new Map();
  }

  registerAgent(name: string, agent: GeminiAgent): void {
    this.agents.set(name, agent);
  }

  async executeSequentialWorkflow(
    workflow: Array<{
      agentName: string;
      instruction: string;
    }>
  ): Promise<TaskResult> {
    let context = "";

    for (const step of workflow) {
      const agent = this.agents.get(step.agentName);
      if (!agent) {
        throw new Error(`Agent ${step.agentName} not found`);
      }

      const instruction = `${step.instruction}\n\n前ステップの結果: ${context}`;
      const result = await agent.execute(instruction);

      context += `\n[${step.agentName}の結果]\n${result}`;
    }

    return {
      success: true,
      data: { finalOutput: context },
      context,
    };
  }
}
```

**パターン2: 並行処理（Parallel Execution）**

```typescript
async function executeParallelAgents(
  agents: Map<string, GeminiAgent>,
  task: string
): Promise<Record<string, string>> {
  const promises: Promise<[string, string]>[] = [];

  for (const [name, agent] of agents) {
    promises.push(agent.execute(task).then((result) => [name, result]));
  }

  const results = await Promise.all(promises);
  return Object.fromEntries(results);
}
```

**パターン3: 動的ルーティング（Dynamic Routing）**

```typescript
class IntelligentRouter {
  private agents: Map<string, GeminiAgent>;

  constructor(agents: Map<string, GeminiAgent>) {
    this.agents = agents;
  }

  async routeTask(input: string): Promise<TaskResult> {
    const routerPrompt = `
ユーザーの要求を分析し、以下のいずれかに分類してください:
1. "sales_query" - 売上データ・販売戦略に関する質問
2. "trend_analysis" - 市場トレンド・競合分析
3. "forecast" - 需要予測・シナリオプランニング

ユーザー入力: "${input}"

分類結果のみを出力してください。
    `;

    const routerAgent = this.agents.get("router")!;
    const classification = await routerAgent.execute(routerPrompt);

    const targetAgent = this.agents.get(
      classification.toLowerCase().trim()
    );
    if (!targetAgent) {
      throw new Error(`No agent for: ${classification}`);
    }

    const result = await targetAgent.execute(input);
    return {
      success: true,
      data: { response: result },
      context: result,
    };
  }
}
```

## セッション管理とコンテキスト保持

```typescript
interface SessionContext {
  sessionId: string;
  userId: string;
  startedAt: Date;
  conversationHistory: Array<{
    agentName: string;
    role: "user" | "assistant";
    message: string;
    timestamp: Date;
  }>;
  sharedMemory: Map<string, any>;
  metadata: Record<string, any>;
}

class SessionManager {
  private sessions: Map<string, SessionContext> = new Map();
  private sessionTTL = 30 * 60 * 1000; // 30分

  createSession(userId: string): SessionContext {
    const sessionId = `sess_${Date.now()}_${Math.random().toString(36)}`;
    const context: SessionContext = {
      sessionId,
      userId,
      startedAt: new Date(),
      conversationHistory: [],
      sharedMemory: new Map(),
      metadata: {},
    };

    this.sessions.set(sessionId, context);

    setTimeout(
      () => this.sessions.delete(sessionId),
      this.sessionTTL
    );

    return context;
  }

  getSession(sessionId: string): SessionContext | undefined {
    return this.sessions.get(sessionId);
  }

  updateSharedMemory(
    sessionId: string,
    key: string,
    value: any
  ): void {
    const session = this.sessions.get(sessionId);
    if (session) {
      session.sharedMemory.set(key, value);
    }
  }
}
```

## エラー処理と回復パターン

```typescript
type RetryStrategy = "exponential_backoff" | "linear" | "fixed";

interface RetryConfig {
  maxAttempts: number;
  strategy: RetryStrategy;
  baseDelayMs: number;
  maxDelayMs: number;
}

async function executeWithRetry(
  fn: () => Promise<TaskResult>,
  config: RetryConfig
): Promise<TaskResult> {
  let lastError: Error | null = null;

  for (let attempt = 0; attempt < config.maxAttempts; attempt++) {
    try {
      return await fn();
    } catch (error) {
      lastError = error as Error;
      console.warn(
        `Attempt ${attempt + 1} failed: ${lastError.message}`
      );

      if (attempt < config.maxAttempts - 1) {
        const delay = calculateDelay(
          attempt,
          config
        );
        await new Promise((resolve) =>
          setTimeout(resolve, delay)
        );
      }
    }
  }

  throw new Error(
    `Failed after ${config.maxAttempts} attempts: ${lastError?.message}`
  );
}

function calculateDelay(
  attempt: number,
  config: RetryConfig
): number {
  let delay: number;

  switch (config.strategy) {
    case "exponential_backoff":
      delay = config.baseDelayMs * Math.pow(2, attempt);
      break;
    case "linear":
      delay = config.baseDelayMs * (attempt + 1);
      break;
    case "fixed":
      delay = config.baseDelayMs;
      break;
  }

  return Math.min(delay, config.maxDelayMs);
}
```

## コスト監視とレート制限

```typescript
interface CostMetrics {
  inputTokens: number;
  outputTokens: number;
  estimatedCostUSD: number;
  timestamp: Date;
}

class CostMonitor {
  private metrics: CostMetrics[] = [];
  private readonly INPUT_COST_PER_1M = 0.00075;
  private readonly OUTPUT_COST_PER_1M = 0.003;
  private dailyBudgetUSD = 10;

  recordUsage(
    inputTokens: number,
    outputTokens: number
  ): CostMetrics {
    const cost =
      (inputTokens * this.INPUT_COST_PER_1M) / 1_000_000 +
      (outputTokens * this.OUTPUT_COST_PER_1M) / 1_000_000;

    const metric: CostMetrics = {
      inputTokens,
      outputTokens,
      estimatedCostUSD: cost,
      timestamp: new Date(),
    };

    this.metrics.push(metric);
    return metric;
  }

  getDailyCost(): number {
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    return this.metrics
      .filter((m) => m.timestamp >= today)
      .reduce((sum, m) => sum + m.estimatedCostUSD, 0);
  }

  isWithinBudget(): boolean {
    return this.getDailyCost() < this.dailyBudgetUSD;
  }
}

class RateLimiter {
  private requestCounts: Map<string, number[]> = new Map();
  private readonly maxRequestsPerMinute = 60;

  async checkAndWait(userId: string): Promise<void> {
    const now = Date.now();
    const oneMinuteAgo = now - 60_000;

    const requests = this.requestCounts
      .get(userId)
      ?.filter((time) => time > oneMinuteAgo) || [];

    if (requests.length >= this.maxRequestsPerMinute) {
      const oldestRequest = Math.min(...requests);
      const waitTime = oldestRequest + 60_000 - now;
      await new Promise((resolve) =>
        setTimeout(resolve, waitTime)
      );
    }

    requests.push(now);
    this.requestCounts.set(userId, requests);
  }
}
```

## 本番環境へのデプロイ

### Cloudflare Workers への デプロイ

```typescript
// wrangler.toml
name = "gemini-adk-agents"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.production]
route = "api.example.com/agents/*"
zone_id = "your_zone_id"

[build]
command = "npm run build"

[[env.production.vars]]
GEMINI_API_KEY = "YOUR_GEMINI_API_KEY"
ENVIRONMENT = "production"
```

```typescript
// src/index.ts
import { Router } from "itty-router";

const router = Router();

router.post("/api/agents/execute", async (request) => {
  const { sessionId, agentName, task } = await request.json();

  try {
    const sessionManager = new SessionManager();
    const session = sessionManager.getSession(sessionId);

    if (!session) {
      return new Response("Session not found", { status: 404 });
    }

    const agent = agents.get(agentName);
    if (!agent) {
      return new Response("Agent not found", { status: 404 });
    }

    const result = await agent.execute(task);
    sessionManager.addHistoryEntry(
      sessionId,
      agentName,
      "assistant",
      result
    );

    return new Response(
      JSON.stringify({ success: true, result }),
      { status: 200, headers: { "Content-Type": "application/json" } }
    );
  } catch (error) {
    return new Response(
      JSON.stringify({
        success: false,
        error: (error as Error).message,
      }),
      { status: 500, headers: { "Content-Type": "application/json" } }
    );
  }
});

export default router;
```

## オブザーバビリティの実装

```typescript
interface TraceSpan {
  traceId: string;
  spanId: string;
  name: string;
  startTime: Date;
  endTime?: Date;
  status: "pending" | "success" | "error";
  attributes: Record<string, any>;
  error?: string;
}

class ObservabilityService {
  private spans: Map<string, TraceSpan> = new Map();

  startSpan(
    traceId: string,
    name: string,
    attributes: Record<string, any> = {}
  ): string {
    const spanId = `span_${Date.now()}_${Math.random()}`;
    const span: TraceSpan = {
      traceId,
      spanId,
      name,
      startTime: new Date(),
      status: "pending",
      attributes,
    };

    this.spans.set(spanId, span);
    return spanId;
  }

  endSpan(
    spanId: string,
    status: "success" | "error" = "success",
    error?: string
  ): void {
    const span = this.spans.get(spanId);
    if (span) {
      span.endTime = new Date();
      span.status = status;
      if (error) span.error = error;

      this.logSpan(span);
    }
  }

  private logSpan(span: TraceSpan): void {
    console.log(
      JSON.stringify({
        traceId: span.traceId,
        spanId: span.spanId,
        name: span.name,
        duration:
          (span.endTime?.getTime() || Date.now()) -
          span.startTime.getTime(),
        status: span.status,
        attributes: span.attributes,
        error: span.error,
      })
    );
  }
}
```

## まとめ

Google ADK と Gemini API を用いたマルチエージェントシステムの構築は、以下のポイントで成功します。

1. **型安全性**: TypeScript の strict モードで未然にバグを防止
2. **通信パターン**: 順序実行・並行処理・動的ルーティングを用途に応じ選択
3. **セッション管理**: 長時間対話でのコンテキスト保持
4. **エラー処理**: 再試行・フォールバック・グレースフルデグラデーション
5. **運用基盤**: コスト監視・レート制限・オブザーバビリティの統合

これらの実装パターンを組み合わせることで、信頼性と拡張性を備えた本番レベルのシステムが実現できます。
