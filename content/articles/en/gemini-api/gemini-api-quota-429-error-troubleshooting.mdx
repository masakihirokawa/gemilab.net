---
title: "How to Fix Gemini API Quota Limits and 429 Errors — Free vs Paid Tier Explained"
slug: "gemini-api-quota-429-error-troubleshooting"
category: "gemini-api"
level: "intermediate"
premium: false
date: "2026-03-29T12:00"
author: "Gemini Lab"
description: "Complete guide to diagnosing and fixing 429 Too Many Requests errors in Gemini API. Learn the difference between RESOURCE_EXHAUSTED errors, quota limits by tier, and how to monitor API usage in Google Cloud Console."
tags: ["gemini-api", "troubleshooting", "rate-limit", "error", "quota"]
---

## Setup and context

You're building a solid application with Gemini API, and suddenly it crashes with a `429 Too Many Requests` error. Service down, users frustrated, and you're not sure why.

This error signals that you've hit the API's quota limit, but that simple explanation masks several deeper causes. Maybe you've outgrown the free tier. Perhaps your retry logic isn't right. Or your quota settings in Google Cloud Console need adjustment. Without understanding the root cause, you'll keep bumping into the same wall.

## What 429 Really Means — And Why RESOURCE_EXHAUSTED Appears

### When You'll See a 429 Error

Three main scenarios trigger 429 errors in Gemini API:

- **RPM (Requests Per Minute) limit**: You've exceeded the number of requests allowed in a single minute
- **RPD (Requests Per Day) limit**: You've exceeded the daily request count
- **TPM (Tokens Per Minute) limit**: You've processed too many tokens in 60 seconds

The free tier has strict thresholds for all three. During development, you might never hit them, but when your app scales, 429 errors appear without warning.

### RESOURCE_EXHAUSTED: The Underlying Status

```json
// What a 429 response looks like
{
  "error": {
    "code": 429,
    "message": "Resource has been exhausted (e.g. check quota).",
    "status": "RESOURCE_EXHAUSTED"
  }
}
```

Inside Google's API infrastructure, `RESOURCE_EXHAUSTED` is the detailed status code, and HTTP 429 is the higher-level representation. When you see "Resource has been exhausted," you've hit a quota limit.

This differs from `INVALID_ARGUMENT` or `PERMISSION_DENIED`, which signal malformed requests or auth issues—not quota problems.

## Free Tier vs. Paid Plan: A Side-by-Side Comparison

### Free Tier Quotas

The free tier is designed for development and testing, not production workloads.

| Quota | Limit | Unit |
| --- | --- | --- |
| Requests Per Minute (RPM) | 60 | requests/minute |
| Requests Per Day (RPD) | 1,500 | requests/day |
| Tokens Per Minute (TPM) | 10,000 | tokens/minute |
| Concurrent Connections | 2 | sessions |

These limits apply to your **entire project**, not per user or app. If you're running multiple applications or scripts under the same project, they share the same quota pool, causing limits to be reached faster.

### Paid Tier (Pay-as-you-go) Quotas

Upgrading to a paid plan dramatically increases these limits:

| Quota | Limit | Unit |
| --- | --- | --- |
| RPM | 1,000–10,000 | requests/minute (varies by model/region) |
| RPD | Unlimited | — |
| TPM | 100,000–1,000,000 | tokens/minute (scales with purchase) |
| Concurrent Connections | 100 | sessions |

Additionally, you can set a **Spend Limit** in Google Cloud Console to prevent surprise bills.

## Monitoring and Configuring Quotas in Google Cloud Console

### Step 1: Identify Your Project in Google AI Studio

First, confirm which project you're using:

1. Visit [Google AI Studio](https://aistudio.google.com)
2. Click "Select project" in the top left
3. Note your project name (e.g., `My First Project`)

### Step 2: Check Quota Status in Google Cloud Console

1. Open [Google Cloud Console](https://console.cloud.google.com)
2. Navigate to "APIs & Services" → "Dashboard"
3. Click "Generative Language API"
4. Select the "Quotas and usage" tab

You'll see your current usage and active quota limits at a glance.

### Step 3: Upgrade to a Paid Plan

1. In Google Cloud Console, go to "Billing" → "Account"
2. Add payment information in the "Budget and usage" section
3. Once complete, your API quotas auto-upgrade

Note: Quota changes may take a few minutes to an hour to take effect after upgrade.

## Emergency Fixes for Active 429 Errors

### Three Techniques You Can Implement Immediately

**1. Exponential Backoff Retry Strategy**

```python
import time
import anthropic

def call_gemini_with_backoff(prompt, max_retries=5):
    client = anthropic.Anthropic(api_key="YOUR_GEMINI_API_KEY")

    for attempt in range(max_retries):
        try:
            response = client.messages.create(
                model="gemini-2.0-flash",
                max_tokens=1024,
                messages=[
                    {"role": "user", "content": prompt}
                ]
            )
            return response.content[0].text
        except Exception as e:
            if "429" in str(e) or "RESOURCE_EXHAUSTED" in str(e):
                # Exponential backoff: wait 2^attempt seconds
                wait_time = 2 ** attempt
                print(f"Rate limited. Waiting {wait_time} seconds...")
                time.sleep(wait_time)
            else:
                raise

    raise Exception("Max retries exceeded")

# Usage example
result = call_gemini_with_backoff("Explain quantum entanglement")
print(result)
```

**2. Request Queuing**

When requests spike unpredictably, queuing ensures even distribution:

```python
import queue
import threading
import time

class GeminiQueue:
    def __init__(self, max_rpm=60):
        self.queue = queue.Queue()
        self.max_rpm = max_rpm
        self.request_times = []
        self.lock = threading.Lock()

    def add_request(self, prompt):
        self.queue.put(prompt)

    def process_requests(self):
        while True:
            prompt = self.queue.get()
            self._rate_limit_check()
            # Actual API call here
            self.queue.task_done()

    def _rate_limit_check(self):
        with self.lock:
            now = time.time()
            # Keep only requests from the last 60 seconds
            self.request_times = [t for t in self.request_times if now - t < 60]

            if len(self.request_times) >= self.max_rpm:
                # Wait until the oldest request is outside the 60-second window
                wait_time = 60 - (now - self.request_times[0])
                time.sleep(wait_time)

            self.request_times.append(now)

# Usage example
gq = GeminiQueue(max_rpm=50)
for i in range(100):
    gq.add_request(f"Question {i}")
```

**3. Enforce Serial Processing on Free Tier**

Free tier limits concurrent connections to 2. Force sequential processing:

```python
import asyncio

async def process_sequentially(prompts):
    """Limit concurrent connections to 1 to avoid free tier limits"""
    semaphore = asyncio.Semaphore(1)

    async def fetch_with_semaphore(prompt):
        async with semaphore:
            # API call
            await asyncio.sleep(1)  # Delay to respect rate limits
            return f"Response to: {prompt}"

    tasks = [fetch_with_semaphore(p) for p in prompts]
    return await asyncio.gather(*tasks)

# Usage example
prompts = ["What is AI?", "How does machine learning work?"]
results = asyncio.run(process_sequentially(prompts))
```

## Long-Term Solutions for Production

### Choosing the Right Paid Plan

When 429 errors become frequent on the free tier, it's time to upgrade:

- **Small apps (< 10K requests/month)**: Free tier + Spend Limit
- **Medium apps (10K–1M requests/month)**: Pay-as-you-go recommended
- **Large apps (> 1M requests/month)**: Contact Google Cloud Sales about volume pricing

### Setting a Spend Limit

Prevent surprise bills by capping your monthly spend:

1. Google Cloud Console → "Billing"
2. Click "Budgets" → "Create budget"
3. Set "Monthly budget" (e.g., ¥10,000)
4. Enable alerts at 100% of budget

Once your monthly spending reaches the limit, all API calls automatically stop.

### Automate Quota Monitoring

Use Google Cloud Monitoring (formerly Stackdriver) to track quota usage:

```yaml
# Google Cloud Monitoring Alert Example
displayName: Gemini API Rate Limit Watch
conditions:
  - displayName: RPM Usage Exceeds 80%
    conditionThreshold:
      filter: |
        resource.type="api"
        AND metric.type="serviceruntime.googleapis.com/api/consumer/quota_used_count"
        AND resource.service="generativelanguage.googleapis.com"
      threshold_value: 0.8
      comparison: COMPARISON_GT
```

## Wrapping Up

Gemini API 429 errors are preventable with proper planning:

1. **Early development**: Understand free tier limits and add retry logic
2. **As you scale**: Upgrade to a paid plan and set a Spend Limit
3. **In production**: Continuously monitor quotas via Google Cloud Console

For deeper context on rate management, read [Gemini API Rate Limiting & Quota Management Guide](/articles/gemini-api/gemini-api-rate-limiting-quota-management). For comprehensive error handling across all scenarios, see [Error Handling & Retry Patterns](/articles/gemini-api/gemini-api-error-handling-retry-patterns).

To master API design practices beyond just error handling, <a href="[www.amazon.co.jp](https://www.amazon.co.jp/dp/B0DGWH9K3L/?tag=pinocchio-22)" target="_blank" rel="noopener">explore API design best practices</a> that help you build resilient systems from the ground up.
