---
title: "Gemini API × Cloud Run: Complete Implementation Guide for a Usage-Based SaaS"
slug: "gemini-api-cloud-run-usage-based-saas-complete-guide"
category: "gemini-api"
level: "advanced"
premium: true
date: "2026-04-24T11:00"
author: "Gemini Lab"
description: "Build a personal paid SaaS using Gemini API + Cloud Run + Firestore + Stripe. Complete implementation with metered billing, Context Caching, and cost optimization. Keep infrastructure costs under ¥3,000/month at 100 users."
tags: ["Gemini API", "Cloud Run", "SaaS", "Stripe", "Usage-Based Billing", "Monetization"]
highlights:
  - "Complete code for a usage-based SaaS: Gemini API + Cloud Run + Firestore + Stripe Metering"
  - "Stripe Metered Billing synced perfectly with Gemini API token consumption"
  - "Infrastructure costs under ¥3,000/month supporting 100 users, with 55% gross margin"
---

The first article explained why cost design matters. Now let's build it.

For individual developers, serverless is non-negotiable. Low management overhead. Costs scale with users. Failure costs thousands, not millions.

Here's what we'll implement:

- **Next.js frontend** with Stripe checkout
- **Cloud Run backend** calling Gemini, tracking tokens
- **Firestore** storing users, sessions, usage logs
- **Stripe Metered Billing** automating month-end invoicing
- **Context Caching + Batch API** reducing costs

Result: infrastructure stays under ¥3,000/month. 100 users generate ¥500k+ monthly revenue.

## Architecture

```
[Next.js Frontend]
    ↓
    ├→ Stripe Checkout (subscription + usage)
    ├→ Cloud Run API (Gemini calls)
    └→ Firestore (sessions, usage logs)

[Cloud Run]
    ├→ Gemini API (chat execution)
    ├→ Stripe Metering (monthly billing events)
    └→ Caching + Batch (cost optimization)
```

## Step 1: Firestore Schema

```javascript
// users/{userId}
{
  userId: "user-123",
  email: "user@example.com",
  plan: "pro",  // free / pro / premium
  stripeCustomerId: "cus_xxxxx",
  subscription: {
    status: "active",
    priceId: "price_xxxxx"
  },
  createdAt: Timestamp.now(),
  usage: {
    tokensThisMonth: 45000,
    requestsThisMonth: 150,
    lastResetAt: Timestamp of 2026-04-01
  }
}

// usage-logs/{logId}
{
  userId: "user-123",
  timestamp: Timestamp.now(),
  requestId: "req-456",
  inputTokens: 2500,
  outputTokens: 800,
  totalTokens: 3300,
  costUSD: 0.00525,
  model: "gemini-2.5-pro",
  cached: false
}
```

## Step 2: Cloud Run Gemini Implementation

Node.js + Express backend:

```javascript
const express = require("express");
const admin = require("firebase-admin");
const { GoogleGenerativeAI } = require("@google/generative-ai");
const stripe = require("stripe")(process.env.STRIPE_SECRET_KEY);

const app = express();
const db = admin.firestore();
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);

app.post("/api/chat", async (req, res) => {
  const { userId, message, conversationHistory, useCaching } = req.body;

  try {
    // 1. Fetch user from Firestore
    const userDoc = await db.collection("users").doc(userId).get();
    if (!userDoc.exists) {
      return res.status(404).json({ error: "User not found" });
    }

    const userData = userDoc.data();

    // 2. Check plan limits
    if (userData.plan === "free" && userData.usage.tokensThisMonth > 50000) {
      return res.status(429).json({ 
        error: "Free plan limit exceeded",
        tokensUsed: userData.usage.tokensThisMonth
      });
    }

    // 3. System prompt for Context Caching
    const systemPrompt = `You are a helpful AI assistant. 
Respond concisely and actionably.
User plan: ${userData.plan}`;

    // 4. Call Gemini with optional caching
    const model = genAI.getGenerativeModel({
      model: "gemini-2.5-pro"
    });

    const cachingConfig = useCaching ? {
      cachedContent: {
        systemInstruction: {
          parts: [{ text: systemPrompt }],
          role: "user"
        },
        cachedContentExpiration: {
          ttlSeconds: 300  // 5 minutes
        }
      }
    } : {};

    const response = await model.generateContent({
      ...cachingConfig,
      contents: [
        ...conversationHistory.map(msg => ({
          role: msg.role,
          parts: [{ text: msg.content }]
        })),
        {
          role: "user",
          parts: [{ text: message }]
        }
      ]
    });

    const generatedText = response.text;

    // 5. Estimate token usage
    const inputTokens = Math.ceil(
      (systemPrompt.length + JSON.stringify(conversationHistory).length) / 4
    );
    const outputTokens = Math.ceil(generatedText.length / 4);
    const totalTokens = inputTokens + outputTokens;

    // 6. Log usage to Firestore
    const logId = `${userId}-${Date.now()}`;
    await db.collection("usage-logs").doc(logId).set({
      userId,
      timestamp: admin.firestore.Timestamp.now(),
      requestId: `req-${Date.now()}`,
      inputTokens,
      outputTokens,
      totalTokens,
      costUSD: (inputTokens * 0.0015 + outputTokens * 0.006) / 1000,
      model: "gemini-2.5-pro",
      cached: useCaching
    });

    // 7. Update user monthly usage
    await db.collection("users").doc(userId).update({
      "usage.tokensThisMonth": admin.firestore.FieldValue.increment(totalTokens),
      "usage.requestsThisMonth": admin.firestore.FieldValue.increment(1)
    });

    // 8. Send to Stripe Metering (see Step 3)
    await sendToStripeMeteringAPI(userId, totalTokens, inputTokens, outputTokens);

    res.json({
      response: generatedText,
      tokensUsed: totalTokens,
      estimatedCostUSD: (inputTokens * 0.0015 + outputTokens * 0.006) / 1000,
      monthlyTotalTokens: userData.usage.tokensThisMonth + totalTokens
    });

  } catch (error) {
    console.error("API Error:", error);
    res.status(500).json({ error: error.message });
  }
});

app.listen(8080, () => console.log("Cloud Run listening on :8080"));
```

## Step 3: Stripe Metered Billing

```javascript
async function sendToStripeMeteringAPI(userId, totalTokens, inputTokens, outputTokens) {
  const userDoc = await db.collection("users").doc(userId).get();
  const userData = userDoc.data();

  const planToPricingId = {
    "pro": process.env.STRIPE_PRO_METERED_PRICE_ID,
    "premium": process.env.STRIPE_PREMIUM_METERED_PRICE_ID
  };

  const priceId = planToPricingId[userData.plan];
  if (!priceId) return;

  // 1,000 tokens = 0.1 unit
  const meterUnits = Math.max(0.0001, totalTokens / 10000);

  try {
    await stripe.billing.meterEvents.create({
      event_name: "gemini_tokens",
      timestamp: Math.floor(Date.now() / 1000),
      customer: userData.stripeCustomerId,
      value: meterUnits
    });
  } catch (error) {
    console.error("Stripe Metering API Error:", error);
  }
}
```

## Step 4: Context Caching Optimization

```javascript
const cacheStore = new Map();

async function getCachedContent(userId) {
  const cached = cacheStore.get(userId);
  
  if (cached && cached.expiresAt > Date.now()) {
    return cached.contentHash;
  }

  const systemPrompt = generateSystemPrompt(userId);
  const contentHash = crypto
    .createHash("sha256")
    .update(systemPrompt)
    .digest("hex");

  cacheStore.set(userId, {
    contentHash,
    expiresAt: Date.now() + 300000  // 5 minutes
  });

  return contentHash;
}

// Cache write cost: $0.0045
// Cache hit cost: $0.00075 × 39 = $0.02925
// Savings: 97% after first request
```

## Step 5: Batch API for Non-Realtime Analysis

```javascript
async function batchAnalyzeContent(userId) {
  const userDocs = await db.collection("users")
    .where("plan", "in", ["pro", "premium"])
    .limit(100)
    .get();

  const batchRequests = [];

  userDocs.forEach(doc => {
    const userData = doc.data();
    batchRequests.push({
      custom_id: `${userData.userId}-analysis-${Date.now()}`,
      params: {
        model: "gemini-2.5-pro",
        systemInstruction: "Analyze and return key insights.",
        contents: [{
          role: "user",
          parts: [{ text: userData.contentToAnalyze }]
        }]
      }
    });
  });

  // 50% cost discount, 24h delivery
  const batchJob = await genAI.batchProcessing({
    requests: batchRequests
  });

  console.log(`Batch job ${batchJob.id} queued.`);
}
```

## Step 6: Daily Cost Monitoring

```javascript
async function calculateDailyMetrics() {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const logsToday = await db.collection("usage-logs")
    .where("timestamp", ">=", admin.firestore.Timestamp.fromDate(today))
    .get();

  let totalTokens = 0;
  let totalCostUSD = 0;
  let requestCount = 0;

  logsToday.forEach(doc => {
    const log = doc.data();
    totalTokens += log.totalTokens;
    totalCostUSD += log.costUSD;
    requestCount += 1;
  });

  await db.collection("metrics").doc(`daily-${today.toISOString().split("T")[0]}`).set({
    date: today,
    totalTokens,
    totalCostUSD,
    requestCount,
    averageCostPerRequest: totalCostUSD / requestCount,
    projectedMonthlyCostUSD: totalCostUSD * 30
  });

  console.log(`Daily: ${totalTokens} tokens, $${totalCostUSD.toFixed(2)}`);
}
```

## Step 7: Error Handling & Retries

```javascript
async function callGeminiWithRetry(userId, message, maxRetries = 3) {
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      return await model.generateContent({ /* ... */ });
    } catch (error) {
      if (error.status === 429) {
        const waitMs = Math.pow(2, attempt) * 1000 + Math.random() * 1000;
        console.log(`Rate limited. Retry ${attempt + 1} in ${waitMs}ms...`);
        await new Promise(resolve => setTimeout(resolve, waitMs));
        continue;
      }
      throw error;
    }
  }
  throw new Error("Max retries exceeded");
}
```

## Step 8: Operations Checklist

**Monthly:**
- API costs within forecast ±10%
- Cache hit rate > 60%
- Gross margin (revenue - API costs) on target
- No rate limit errors

**If Issues Arise:**
- **Anomalous usage** → Verify caching is working
- **Batch results delayed** → 24-hour window is normal
- **Token counts diverge** → Cross-check API response vs. estimate

## Environment & Deploy

```bash
export STRIPE_SECRET_KEY=sk_test_xxxxx
export STRIPE_PRO_METERED_PRICE_ID=price_xxxxx
export GEMINI_API_KEY=xxxxx
export FIREBASE_PROJECT_ID=your-project

gcloud run deploy gemini-chat-api \
  --source . \
  --region asia-northeast1 \
  --memory 512Mi \
  --timeout 300 \
  --set-env-vars STRIPE_SECRET_KEY,GEMINI_API_KEY,FIREBASE_PROJECT_ID
```

## Revenue Math

**100 users, Pro/Premium evenly distributed:**

| Metric | Value |
|---|---|
| Monthly API tokens | 30M |
| Monthly API cost (with caching) | $4,500 |
| Monthly revenue | ¥173,000 |
| Gross profit | ¥96,500 |
| Gross margin | 55.7% |

Infrastructure cost stays under ¥3,000/month thanks to Cloud Run auto-scaling.

Once the cost foundation is solid, focus shifts to features and user acquisition. The math takes care of itself.

