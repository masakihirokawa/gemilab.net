---
title: "Gemini Practical Techniques [Part 2] — Production Systems, Voice AI & Advanced API Monetization"
slug: "gemini-practical-techniques-part2"
category: "gemini-basics"
level: "advanced"
premium: true
date: "2026-03-20T14:20"
author: "Gemini Lab"
description: "Advanced techniques from Gemini Lab premium articles. Part 2 covers production agent systems, Live API voice AI, Vertex AI, Veo 3, and SaaS monetization pipelines."
tags: ["Gemini", "practical-techniques", "advanced", "production", "voice-AI", "Live API", "monetization", "part-2", "premium"]
highlights:
  - "Gemini practical techniques part 2"
  - "Gemini utilization patterns by business type and industry"
  - "Productivity improvement and new business opportunities"

---

## Introduction — Part 2 Topics

Part 2 covers advanced techniques for running Gemini in production and generating business value. We build on Part 1 fundamentals to focus on:

- **System-Level Design**: High-traffic handling, error management, monitoring
- **Voice and Multimodal**: Real-time interaction via Live API
- **Vertex AI Integration**: Production deployment on Google Cloud with scaling
- **Video and Fine-tuning**: Advanced ML capabilities
- **Monetization**: API-based revenue pipelines

This guide is based on patterns from actual production systems across startups and enterprises.

---

## Production-Grade Agent Systems

### Gemini 2.5 Pro Parallel Tool Calling

Execute multiple tasks simultaneously to drastically reduce latency. Gemini 2.5 Pro natively supports parallel tool calling:

```python
import asyncio
import google.generativeai as genai
from concurrent.futures import ThreadPoolExecutor

# Tool definitions: Multiple external API calls
tools = [
    {
        "name": "get_weather",
        "description": "Get weather info for specified city",
        "input_schema": {
            "type": "object",
            "properties": {
                "city": {"type": "string"}
            },
            "required": ["city"]
        }
    },
    {
        "name": "get_travel_cost",
        "description": "Calculate transportation cost between cities",
        "input_schema": {
            "type": "object",
            "properties": {
                "origin": {"type": "string"},
                "destination": {"type": "string"}
            },
            "required": ["origin", "destination"]
        }
    },
    {
        "name": "get_hotel_price",
        "description": "Get average hotel price",
        "input_schema": {
            "type": "object",
            "properties": {
                "city": {"type": "string"},
                "stars": {"type": "integer", "enum": [3, 4, 5]}
            },
            "required": ["city", "stars"]
        }
    }
]

# Implementation
def get_weather(city: str) -> dict:
    """Details omitted"""
    return {"city": city, "temp": 25, "condition": "sunny"}

def get_travel_cost(origin: str, destination: str) -> dict:
    return {"route": f"{origin}→{destination}", "cost": 12000}

def get_hotel_price(city: str, stars: int) -> dict:
    return {"city": city, "stars": stars, "avg_price": 15000 * stars}

# Model initialization
model = genai.GenerativeModel("gemini-2.5-pro", tools=tools)

# User request
prompt = "Plan my trip from Tokyo to Osaka. I need weather, transport cost, and 5-star hotel info."

# Parallel execution handler
def execute_parallel_tools(tool_calls: list) -> list:
    """Execute multiple tool calls in parallel"""
    results = []

    with ThreadPoolExecutor(max_workers=5) as executor:
        futures = {}

        for call in tool_calls:
            if call.name == "get_weather":
                future = executor.submit(get_weather, call.args["city"])
            elif call.name == "get_travel_cost":
                future = executor.submit(
                    get_travel_cost,
                    call.args["origin"],
                    call.args["destination"]
                )
            elif call.name == "get_hotel_price":
                future = executor.submit(
                    get_hotel_price,
                    call.args["city"],
                    call.args["stars"]
                )

            futures[call.name] = future

        # Wait for all results
        for tool_name, future in futures.items():
            results.append({"tool": tool_name, "result": future.result()})

    return results

# Process AI response
response = model.generate_content(prompt)

# Handle parallel tool calls if present
parallel_results = []
for part in response.content:
    if hasattr(part, 'function_call'):
        pass

# Complete implementation
print("Trip planning preparation complete...")
```

**Important:** Gemini 2.5 Pro returns `function_call_list` with multiple tool invocations. App-side parallel execution means total latency equals the slowest tool call.

**Performance Comparison:**
```
Sequential: get_weather (1s) + get_travel_cost (0.8s) + get_hotel_price (0.7s) = 2.5s
Parallel: max(1s, 0.8s, 0.7s) = 1s
```

### Error Handling and Retry Strategy

Production environments experience network delays, rate limits, and timeouts:

```python
import time
from typing import Optional, Dict, Any
from tenacity import (
    retry,
    wait_exponential,
    stop_after_attempt,
    retry_if_exception_type,
)

class GeminiAPIError(Exception):
    pass

@retry(
    wait=wait_exponential(multiplier=1, min=2, max=10),  # Exponential backoff
    stop=stop_after_attempt(3),  # Max 3 retries
    retry=retry_if_exception_type((GeminiAPIError, Exception)),
    reraise=True
)
def call_gemini_with_retry(
    prompt: str,
    max_tokens: int = 1024
) -> Optional[str]:
    """API call with retry logic"""
    try:
        model = genai.GenerativeModel("gemini-2.5-pro")
        response = model.generate_content(
            prompt,
            generation_config=genai.types.GenerationConfig(
                max_output_tokens=max_tokens,
                temperature=0.7
            )
        )
        return response.text

    except genai.types.APIError as e:
        if e.status_code == 429:  # Rate limit
            raise GeminiAPIError(f"Rate limited: {e}") from e
        elif e.status_code == 500:  # Server error
            raise GeminiAPIError(f"Server error: {e}") from e
        else:
            raise

    except Exception as e:
        raise GeminiAPIError(f"Unexpected error: {e}") from e


# Usage
try:
    result = call_gemini_with_retry("Explain Japan's GNP")
    print(result)
except GeminiAPIError as e:
    print(f"Failed after 3 retries: {e}")
```

**Best Practices:**
1. Use exponential backoff for rate limits
2. Long wait times for 429 errors
3. Retry 5xx errors
4. Don't retry 4xx errors (400, 401, 403)
5. Set appropriate timeouts (30-60 seconds typical)

---

## Live API and Voice AI

### Real-time Voice Assistant via Live API

Gemini 2.5 Pro's Live API enables real-time voice dialogue over WebSocket:

```python
import asyncio
import websockets
import json
import base64
import sounddevice as sd
import numpy as np

async def audio_assistant():
    """Real-time voice conversation agent"""

    # WebSocket URI (see Google AI API docs)
    ws_uri = "wss://generativelanguage.googleapis.com/google.ai.generativelanguage.v1alpha.GenerativeService/BidiGenerateContent"

    async with websockets.connect(
        ws_uri,
        subprotocols=["json", "proto"],
        extra_headers={"Authorization": f"Bearer {GEMINI_API_KEY}"}
    ) as websocket:

        # Initialize session
        setup_msg = {
            "setup": {
                "model": "models/gemini-2.5-pro",
                "generation_config": {
                    "temperature": 0.7,
                    "max_output_tokens": 1024
                },
                "system_instruction": "You are a helpful Japanese-speaking AI assistant."
            }
        }
        await websocket.send(json.dumps(setup_msg))

        # Receive init response
        init_response = await websocket.recv()
        print(f"Init response: {init_response}")

        # Audio input thread
        async def send_audio():
            """Send mic input"""
            print("Voice assistant started. Speak now...")

            # Audio capture (16kHz, mono)
            sample_rate = 16000
            chunk_duration = 0.5  # Send every 500ms
            chunk_samples = int(sample_rate * chunk_duration)

            with sd.InputStream(
                channels=1,
                samplerate=sample_rate,
                dtype=np.int16
            ) as stream:
                while True:
                    audio_chunk, _ = stream.read(chunk_samples)

                    # Encode to binary
                    audio_bytes = audio_chunk.tobytes()
                    audio_b64 = base64.b64encode(audio_bytes).decode()

                    # Send to Gemini
                    msg = {
                        "realtime_input": {
                            "media_chunks": [
                                {
                                    "data": audio_b64,
                                    "mime_type": "audio/pcm"
                                }
                            ]
                        }
                    }
                    await websocket.send(json.dumps(msg))
                    await asyncio.sleep(chunk_duration)

        # Audio output thread
        async def receive_audio():
            """Receive Gemini responses"""
            while True:
                try:
                    response = await asyncio.wait_for(websocket.recv(), timeout=1.0)
                    data = json.loads(response)

                    # Text response
                    if "server_content" in data:
                        for part in data["server_content"]["parts"]:
                            if "text" in part:
                                print(f"AI: {part['text']}")

                    # Audio response (TTS)
                    if "server_content" in data:
                        for part in data["server_content"]["parts"]:
                            if "inline_data" in part:
                                audio_b64 = part["inline_data"]["data"]
                                audio_bytes = base64.b64decode(audio_b64)
                                # Speaker output
                                sd.play(
                                    np.frombuffer(audio_bytes, np.int16),
                                    samplerate=16000
                                )

                except asyncio.TimeoutError:
                    continue
                except Exception as e:
                    print(f"Error: {e}")
                    break

        # Run concurrently
        send_task = asyncio.create_task(send_audio())
        recv_task = asyncio.create_task(receive_audio())

        await asyncio.gather(send_task, recv_task)

# Execute
asyncio.run(audio_assistant())
```

**Setup:**
```bash
pip install websockets sounddevice numpy
export GEMINI_API_KEY="your-api-key"
python audio_assistant.py
```

---

## Advanced API Features

### Fine-tuning for Domain-Specific Models

Create specialized models for healthcare, law, tech documentation:

```python
from google.cloud import aiplatform

# Prepare training data
training_data = [
    {
        "input": "Patient reports headache. Recent stress level is high.",
        "output": "Likely stress-related headache. Recommend...",
        "weight": 1.0
    },
    {
        "input": "Blood sugar 150 mg/dL at 2 hours post-meal.",
        "output": "Slightly elevated. If diabetic, consult doctor."
    },
    # 100-1000 more examples
]

# Execute fine-tuning on Vertex AI
model = aiplatform.GenerativeModel(
    model_name="gemini-2.5-pro"
)

tuning_job = model.tune(
    training_data=training_data,
    epoch_count=3,
    learning_rate_multiplier=1.0,
    display_name="Medical-Gemini-2.5-Pro",
)

# Wait for completion
tuned_model_name = tuning_job.result().name
print(f"Fine-tuning complete: {tuned_model_name}")

# Use tuned model
tuned_model = aiplatform.GenerativeModel(model_name=tuned_model_name)
response = tuned_model.generate_content(
    "Patient reports 3-day continuous abdominal pain"
)
print(response.text)
```

### Context Caching for Cost Optimization

Reuse large inputs (PDFs, long guides) across multiple queries:

```python
import google.generativeai as genai
from google.generativeai import caching

# Large system prompt
system_prompt = """
You are an expert in Japanese business law.
Follow these guidelines:
1. Always cite relevant law articles
2. Provide practical advice
3. Acknowledge uncertainty
...(500-1000 lines of detailed guidelines)
"""

# Set cache policy (24 hours)
cache_config = caching.CachedContent(
    model="models/gemini-2.5-pro",
    system_instruction=system_prompt,
    ttl_seconds=86400,  # 24 hours
)

# Create cache
cached_content = genai.caching.cached_content.create(cache_config)
cache_id = cached_content.name
print(f"Cache created: {cache_id}")

# Request with cache (major cost savings)
model = genai.GenerativeModel(
    "gemini-2.5-pro",
    cached_content=cache_id
)

# Multiple user requests
queries = [
    "Reasons for contract invalidity",
    "Director liability issues",
    "Latest labor law changes"
]

for query in queries:
    response = model.generate_content(query)
    print(f"Q: {query}\nA: {response.text}\n---")

# Cost comparison:
# No cache: 1000 tokens x 3 = 3000 charged
# With cache: setup (1000) + 20 tokens x 3 = 1060 (65% savings)
```

### Veo 3 Video Generation

Generate videos from text prompts (Gemini integration):

```python
from google.cloud import generativelanguage_v1
import os

def generate_video_from_text(prompt: str, duration_seconds: int = 6) -> str:
    """Generate video from text prompt"""

    client = generativelanguage_v1.GenerativeServiceClient()

    request = generativelanguage_v1.GenerateContentRequest(
        model="models/veo-3",  # Video generation model
        contents=[
            generativelanguage_v1.Content(
                parts=[
                    generativelanguage_v1.Part(
                        text=prompt
                    )
                ]
            )
        ],
        generation_config=generativelanguage_v1.GenerationConfig(
            temperature=0.8,
            max_output_tokens=2048,
            candidate_count=1,  # Generate 1 video
        )
    )

    response = client.generate_content(request)

    # Get video URL
    video_url = response.candidates[0].content.parts[0].file_data.uri
    return video_url

# Usage
prompt = """
Kyoto's Kiyomizu-dera temple in spring with cherry blossoms.
Tourists walking the stone path toward the temple.
Slow, deliberate camera movement.
Japanese traditional beauty.
"""

video_url = generate_video_from_text(prompt, duration_seconds=8)
print(f"Generated video: {video_url}")

# Video can be downloaded, edited, published
```

**Use Cases:**
- Travel guides and tourism promos
- Product demo videos
- YouTube content drafts
- Business presentation assets

---

## Vertex AI Production Deployment

### Personal Intelligence API for Customization

Personalize responses based on user profiles:

```python
from google.cloud import aiplatform

# User profile
user_profile = {
    "user_id": "user_12345",
    "preferences": {
        "interests": ["AI", "travel", "cooking"],
        "language": "ja",
        "location": "Tokyo",
        "budget_level": "high"
    },
    "purchase_history": [
        {"product": "Kindle", "date": "2025-06-01"},
        {"product": "MacBook Pro", "date": "2025-03-15"}
    ]
}

# Customize with Personal Intelligence API
model = aiplatform.GenerativeModel("gemini-2.5-pro")

response = model.generate_content(
    input_text="Suggest where to vacation next",
    user_profile=user_profile,  # Personalization data
)

print(response.text)

# Response example:
# "Given your high tech interest and budget, consider
#  Silicon Valley tech tours or Taiwan's AI research centers.
#  Also suggesting cooking experiences given your interests..."
```

---

## SaaS Monetization Pipeline

### Gemini API + Stripe for Monetization

Monetize AI services based on usage:

```python
import stripe
import google.generativeai as genai
from functools import wraps
from datetime import datetime, timedelta

# Stripe setup
stripe.api_key = "sk_live_xxx"

# Track API usage
class APIUsageTracker:
    def __init__(self):
        self.usage_log = {}  # {user_id: {date: tokens_used}}

    def track_usage(self, user_id: str, tokens: int):
        """Log user API usage"""
        today = str(datetime.now().date())
        if user_id not in self.usage_log:
            self.usage_log[user_id] = {}
        if today not in self.usage_log[user_id]:
            self.usage_log[user_id][today] = 0
        self.usage_log[user_id][today] += tokens

    def get_monthly_usage(self, user_id: str) -> int:
        """Get monthly token usage"""
        total = 0
        today = datetime.now().date()
        month_start = today.replace(day=1)
        for date_str, tokens in self.usage_log.get(user_id, {}).items():
            date = datetime.strptime(date_str, "%Y-%m-%d").date()
            if date >= month_start:
                total += tokens
        return total

tracker = APIUsageTracker()

# Usage-based billing
def charge_api_usage(user_id: str, tokens_used: int):
    """Charge based on usage"""
    # Pricing: 1M tokens = $20
    cost = (tokens_used / 1_000_000) * 20

    # Get user's Stripe Customer
    customer_id = get_customer_id(user_id)  # Implementation omitted

    # Charge
    if cost > 0.01:  # Minimum $0.01
        charge = stripe.Charge.create(
            amount=int(cost * 100),  # Cents
            currency="usd",
            customer=customer_id,
            description=f"Gemini API usage - {tokens_used} tokens"
        )
        print(f"Charged {user_id} for {tokens_used} tokens: ${cost}")

    # Track usage
    tracker.track_usage(user_id, tokens_used)

# Auto-billing decorator
def requires_payment(func):
    @wraps(func)
    def wrapper(user_id: str, *args, **kwargs):
        # Monthly limit check
        monthly_usage = tracker.get_monthly_usage(user_id)
        if monthly_usage > 100_000_000:
            print(f"Warning: {user_id} exceeded free tier. Charging for overage.")

        result = func(user_id, *args, **kwargs)

        # Charge for usage
        if hasattr(result, "usage_metadata"):
            charge_api_usage(user_id, result.usage_metadata.output_tokens)

        return result

    return wrapper

@requires_payment
def generate_content_with_billing(user_id: str, prompt: str) -> str:
    """Billable API call"""
    model = genai.GenerativeModel("gemini-2.5-pro")
    response = model.generate_content(prompt)
    response.usage_metadata = response._raw.usage_metadata
    return response

# Usage
result = generate_content_with_billing(
    user_id="user_123",
    prompt="Explain async programming in Python"
)
print(result.text)
```

---

## Related Premium Articles

This guide is an entry point to Gemini Lab's premium library. For deeper learning:

1. **Vertex AI Orchestration Complete Guide** — Cloud Run auto-scaling, BigQuery integration, Firestore real-time sync
2. **Gemini Fine-tuning Bootcamp** — Medical diagnosis AI, legal consultation AI, industry-specific optimization
3. **Live API Advanced** — Long-form conversation, emotion recognition, real-time translation
4. **Gemini SaaS Startup Package** — MVP to Series A funding, team scaling, revenue optimization dashboard
5. **Veo 3 × Gemini** — Auto-scenario generation, auto-editing, YouTube optimization pipeline

---

## Summary

2026's AI business is about **implementation and iteration**, not just technology mastery. Gemini Lab provides that implementation foundation.

**Next Steps:**
- Join Gemini Lab community (Discord)
- Apply for premium article access
- Get mentorship for your project
- Share your work with the community

The AI business landscape moves fast. Master Gemini now.

For a deeper dive into this topic, we recommend [The Art of Prompt Engineering with ChatGPT](https://www.amazon.co.jp/dp/1394286791/?tag=pinocchio-22) by Nathan Hunter. It offers a comprehensive guide to prompt engineering techniques.
